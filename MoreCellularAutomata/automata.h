#pragma once

 namespace Automata {


	//Initialize an array of 79 bool variables
	void initArray(bool arr[79]);

	//Filter through the array and print based on value
	void printArray(bool arr[79]);

	//Apply a rule and update array accordingly
	void updateArray(bool arr[79], int rule);

}